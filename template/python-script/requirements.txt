# Installed with "pip install -r requirements.txt" in the py-cloudhpc venv
# before every .py script runs. Keep only what you import: every package is
# downloaded at the start of the job. Pin versions (numpy==2.1.3) for
# reproducible runs.
numpy
matplotlib
